# This repository is published verbatim by GitHub Pages.
# Everything committed here is PUBLIC. Keep drafts, notes and credentials out.

# --- secrets & local config ---
.env
.env.*
*.pem
*.key
*.p12
*.keystore
secrets.*
credentials.*
*.credentials
.netrc
.htpasswd

# --- editor / IDE / tooling ---
.idea
.vscode
*.swp
*.swo
*~
.claude/
.venv
venv/
__pycache__/
*.py[cod]
node_modules

# --- OS cruft ---
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# --- marketing collateral: lives here for convenience, must never be served ---
# Untracked files are never deployed by GitHub Pages, so ignoring is sufficient.
marketing/
*.linkedin.md

# --- working files that must never be published ---
*.log
*.orig
*.rej
*.bak
*.tmp
*.old
draft-*
DRAFT*
scratch/
scratchpad/
private/
notes/
TODO.local.md

# --- generated / oversized sources that do not belong on the site ---
# The published prototype lives at demos/invoicer/index.html and IS tracked.
# Its 250 KB unbundled source and the design-handoff working copies are not.
*.dc.html
*[Ss]tandalone.html
