# robots.txt for https://hegemann.tech/
#
# This file is authoritative. robots.txt is resolved per ORIGIN, and since the
# site moved from the GitHub Pages project path (niklasjosq.github.io/website/)
# to its own apex domain, this file now sits at the origin root — the exact URL
# standards-compliant crawlers fetch. The per-page <meta name="robots"> tags are
# still in place and remain the finer-grained control.

User-agent: *
Allow: /

# The interactive prototype at /demos/invoicer/ is deliberately NOT disallowed
# here. It already carries <meta name="robots" content="noindex, follow">, and a
# crawler must be able to fetch the page to see that directive. Disallowing it
# would hide the noindex and could still leave a URL-only entry in search results.
# Prefer /invoicer.html, which is the page written for human readers.

# Machine-readable site overview for AI agents:
# https://hegemann.tech/llms.txt

Sitemap: https://hegemann.tech/sitemap.xml
